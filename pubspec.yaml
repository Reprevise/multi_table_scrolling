name: multi_table_scrolling
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.5 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  linked_scroll_controller: ^0.2.0
  two_dimensional_scrollables: ^0.0.4

dev_dependencies:
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
